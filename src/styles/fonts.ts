import { css } from '@emotion/react';

const display1 = css`
  font-size: 2.4rem;
  line-height: 3rem;
  font-weight: 700;
`;
const display2 = css`
  font-size: 2.4rem;
  line-height: 3rem;
  font-weight: 500;
`;

const headline1 = css`
  font-size: 2rem;
  line-height: 2.5rem;
  font-weight: 700;
`;
const headline2 = css`
  font-size: 1.6rem;
  line-height: 2rem;
  font-weight: 700;
`;

const body1 = css`
  font-size: 1.6rem;
  line-height: 2rem;
  font-weight: 500;
`;
const body2 = css`
  font-size: 1.4rem;
  line-height: 2rem;
  font-weight: 500;
`;

const caption1 = css`
  font-size: 1.2rem;
  line-height: 1.5rem;
  font-weight: 500;
`;
const caption2 = css`
  font-size: 1rem;
  line-height: 1.5rem;
  font-weight: 500;
`;

export {
  body1,
  body2,
  caption1,
  caption2,
  display1,
  display2,
  headline1,
  headline2,
};
